# Copy to .env and fill in. .env is gitignored; never commit real values.

# Google Books API key. Required in practice: Google quotas keyless access to zero
# requests per day, so book search cannot reach the live API without one.
# Create at https://console.cloud.google.com/apis/library/books.googleapis.com
GOOGLE_BOOKS_API_KEY=
